<form method="post" action="{{ path('app_music_genre_delete', {'id': music_genre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ music_genre.id) }}">
    <button class="btn">Delete</button>
</form>
