{% extends 'adminbase.html.twig' %}

{% block title %}MusicGenre{% endblock %}

{% block body %}
    <h1>MusicGenre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ music_genre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ music_genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_music_genre_index') }}">back to list</a>

    <a href="{{ path('app_music_genre_edit', {'id': music_genre.id}) }}">edit</a>

    {{ include('music_genre/_delete_form.html.twig') }}
{% endblock %}
