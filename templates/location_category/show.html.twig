{% extends 'base.html.twig' %}

{% block title %}LocationCategory{% endblock %}

{% block body %}
    <h1>LocationCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ location_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_location_category_index') }}">back to list</a>

    <a href="{{ path('app_location_category_edit', {'id': location_category.id}) }}">edit</a>

    {{ include('location_category/_delete_form.html.twig') }}
{% endblock %}
